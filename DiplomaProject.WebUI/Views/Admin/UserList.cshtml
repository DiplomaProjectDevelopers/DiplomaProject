@model IEnumerable<UserViewModel>
@{
    ViewData["Title"] = "Օգտատերերի ցուցակ";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div>
    @Html.Partial("_BaseAdminSideMenu")
    <div class="table-responsive col-md-9 col-sm-8">
        <div>
            <h4 class="col-md-6">Ներկայացված են բոլոր ներկայիս օգտատերերը</h4>
            <div class="btn-group-sm">
                <button class="btn btn-default" disabled>Ցույց տալ միայն ընտրվածները</button>
                <button class="btn btn-danger" disabled>Ջնջել ընտրվածները</button>
            </div>
        </div>
        @if (TempData.ContainsKey("UserDeleted"))
        {<label class="label-info">@TempData["UserDeleted"]</label>}
        @if (TempData.ContainsKey("UserUpdated"))
        {<label class="label-info">@TempData["UserUpdated"]</label>}

        <table id="user-tbl" class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th><input type="checkbox" id="checkall" onchange="oncheck()"/></th>
                    <th>@Html.DisplayNameFor(model => model.Username)</th>
                    <th>@Html.DisplayNameFor(model => model.FirstName)</th>
                    <th>@Html.DisplayNameFor(model => model.LastName)</th>
                    <th>@Html.DisplayNameFor(model => model.Email)</th>
                    <th>@Html.DisplayNameFor(model => model.SelectedRoleId)</th>
                    <th>Խմբագրել</th>
                    <th>Հեռացնել</th>
                </tr>

            </thead>
            <tbody>
                @foreach (var user in Model)
                {
                    <tr>
                        <td>
                            <input type="checkbox" class="checkthis" value="@user.Id" onchange="oncheck()"/>
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => user.Username)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => user.FirstName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => user.LastName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => user.Email)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => user.PhoneNumber)
                        </td>
                        <td>
                            <text>@(user.Gender == null ? "" : user.Gender == true ? "Արական" : "Իգական")</text>
                        </td>
                        <td>
                            <select id="@("roleList"+@user.Id)" asp-for="@user.SelectedRoleId" asp-items="@(new SelectList(ViewBag.Roles, "Id","Name", user.SelectedRoleId))">
                                <option>-- ընտրեք նոր դեր --</option>
                            </select>
                            <a id=@("linkSubmit" + user.Id) class="btn btn-success btn-xs btnSubmitRole" data-title="Հաստատել" data-toggle="modal">
                                <span class="glyphicon glyphicon-ok"></span>
                            </a>
                        </td>
                        <td>
                            <p data-placement="top" data-toggle="tooltip" title="Խմբագրել">
                                <a asp-action="Edit" asp-route-userId="@user.Id" class="btn btn-primary btn-xs" data-title="Խմբագրել" data-toggle="modal">
                                    <span class="glyphicon glyphicon-pencil"></span>
                                </a>
                            </p>
                        </td>
                        <td>
                            <p data-placement="top" data-toggle="tooltip" title="Հեռացնել">
                                <a asp-action="Delete" asp-route-userId="@user.Id" class="btn btn-danger btn-xs" data-title="Հեռացնել" data-toggle="modal">
                                    <span class="glyphicon glyphicon-trash"></span>
                                </a>
                            </p>
                        </td>

                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>>
@section Scripts{
    <script src="~/custom_scripts/UserList.js">
    </script>}

@section Styles{
    <link href="~/custom_contents/_BaseAdminSideMenu.css" />
}