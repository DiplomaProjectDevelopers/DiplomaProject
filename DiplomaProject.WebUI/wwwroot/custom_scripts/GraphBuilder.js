"use strict"
//var Dracula = require('../wwwroot/lib/graphdracula/index');
//var Dracula = require('../lib/graphdracula/dist/dracula.dev')
import { Graph } from '../lib/graphdracula/dist/dracula.dev'
export default function buildGraph(array) {
    if (Graph) {
        const Graph = Graph;
        console.log(Graph);
    }
}